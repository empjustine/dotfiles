#!/bin/sh

AUTOLOAD_FOLDER=${1}

if [ -d "${AUTOLOAD_FOLDER}" ]; then
        for f in ${AUTOLOAD_FOLDER}/*; do
                [ -x "$f" ] && echo ". \"$f\"" && . "$f"
        done
        unset f
fi

unset AUTOLOAD_FOLDER
